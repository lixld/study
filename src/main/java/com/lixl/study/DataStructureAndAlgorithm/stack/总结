栈是一种操作受限的线性表。只允许在一端插入和删除数据。

这种特定的数据结构是为了特定的业务场景设计的。而且数据和链表暴露了太多的操作接口。操作是确实灵活。但同时也不可控。自然容易出错

当某个数据集合只涉及在一端插入和删除数据，并且满足后进先出，先进后出的特性，我们就应该首选"栈"这种数据结构

栈就两个操作
入栈:在栈顶插入一个数据
出栈:从栈顶删除一个数据

实现：
用数组实现：顺序栈
用链表实现：链式栈


